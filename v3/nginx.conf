
#user  nobody;
worker_processes  1;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

    server {
        listen       8080;
        server_name  localhost;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location /hello {
            proxy_pass http://localhost:5000/hello;
        }

        location ~ ^/read/.+ {
            proxy_pass http://localhost:5000;
        }

        location /book/datatables/active {
            proxy_pass http://localhost:5000/hello;
        }
        
        location ~ ^/term/.+ {
            proxy_pass http://localhost:5000;
        }

        # Lute src/Controller routes defined in Symfony to strangle over to Flask.
        # Almost all controllers add their own prefix.
        #
        # /css, /icn, /img, /iui, /js, /favicon.ico, /favicon_dev.ico
        #
        #
        # BingImageSearchController.php:#[Route('/bing')]
        # ('/search/{langid}/{text}/{searchstring?-}', name: 'app_bing_search', methods: ['GET'])]
        # ('/save', name: 'app_bing_save', methods: ['POST'])]
        #
        # TermController.php:#[Route('/term')]
        # ('/index/{search?}', name: 'app_term_index', methods: ['GET'])]
        # ('/datatables', name: 'app_term_datatables', methods: ['POST'])]
        # ('/bulk_set_parent', name: 'app_term_bulk_set_parent', methods: ['POST'])]
        # ('/search/{text}/{langid}', name: 'app_term_search', methods: ['GET'])]
        # ('/new', name: 'app_term_new', methods: ['GET', 'POST'])]
        # ('/sentences/{LgID}/{text}', name: 'app_term_sentences', methods: ['GET'])]
        # ('/{id}', name: 'app_term_show', methods: ['GET'])]
        # ('/{id}/edit', name: 'app_term_edit', methods: ['GET', 'POST'])]
        # ('/{id}', name: 'app_term_delete', methods: ['POST'])]
        #
        # BookController.php:#[Route('/book')]
        # ('/index/{search?}', name: 'app_book_index', methods: ['GET'])]
        # ('/datatables/active', name: 'app_book_datatables_active', methods: ['POST'])]
        # ('/datatables/archived', name: 'app_book_datatables_archived', methods: ['POST'])]
        # ('/archived', name: 'app_book_archived', methods: ['GET'])]
        # ('/read/{BkID}', name: 'app_book_read', methods: ['GET'])]
        # ('/new', name: 'app_book_new', methods: ['GET', 'POST'])]
        # ('/import_webpage', name: 'app_book_import_webpage', methods: ['GET', 'POST'])]
        # ('/{BkID}/delete', name: 'app_book_delete', methods: ['POST'])]
        # ('/{BkID}/archive', name: 'app_book_archive', methods: ['POST'])]
        # ('/{BkID}/unarchive', name: 'app_book_unarchive', methods: ['POST'])]
        # ('/{BkID}/reparse', name: 'app_book_reparse', methods: ['POST'])]
        # ('/{BkID}/edit', name: 'app_book_edit', methods: ['GET', 'POST'])]
        #
        # ImageController.php:#[Route('/userimages')]
        # ('/{lgid}/{term}', name: 'app_userimages', methods: ['GET'])]
        #
        # CacheClearController.php:    #[Route('/cache/clear', name: 'app_cache_clear', methods: ['GET'])]
        #
        # DemoController.php:    #[Route('/demo/done', name: 'app_demo_done', methods: ['GET'])]
        #
        # TextTagController.php:#[Route('/texttag')]
        # ('/jsonlist', name: 'app_texttag_jsonlist', methods: ['GET'])]
        #
        # MapParentsController.php:#[Route('/mapparents')]
        # ('/index', name: 'app_mapparents_index', methods: ['GET'])]
        # ('/export_language/{LgID}', name: 'app_mapparents_language_export', methods: ['GET'])]
        # ('/export_book/{BkID}', name: 'app_mapparents_book_export', methods: ['GET'])]
        # ('/import/{LgID}', name: 'app_mapparents_import', methods: ['GET'])]
        #
        # TermTagController.php:#[Route('/termtag')]
        # ('/jsonlist', name: 'app_termtag_jsonlist', methods: ['GET'])]
        # ('/index', name: 'app_termtag_index', methods: ['GET'])]
        # ('/datatables', name: 'app_termtag_datatables', methods: ['POST'])]
        # ('/new', name: 'app_termtag_new', methods: ['GET', 'POST'])]
        # ('/{id}/delete', name: 'app_termtag_delete', methods: ['POST'])]
        # ('/{id}/edit', name: 'app_termtag_edit', methods: ['GET', 'POST'])]
        #
        # IndexController.php:    #[Route('/', name: 'app_index', methods: ['GET'])]
        # ('/server_info', name: 'app_server_info', methods: ['GET'])]
        #
        # LanguageController.php:#[Route('/language')]
        # ('/index', name: 'app_language_index', methods: ['GET'])]
        # ('/new/{template?-}', name: 'app_language_new', methods: ['GET', 'POST'])]
        # ('/{LgID}/edit', name: 'app_language_edit', methods: ['GET', 'POST'])]
        # ('/{LgID}', name: 'app_language_delete', methods: ['POST'])]
        # ('/jsonlist', name: 'app_language_jsonlist', methods: ['GET'])]
        #
        # TermImportController.php:#[Route('/termimport')]
        # ('/index', name: 'app_term_import_index', methods: ['GET', 'POST'])]
        #
        # TextController.php:#[Route('/text')]
        # ('/{TxID}/edit', name: 'app_text_edit', methods: ['GET', 'POST'])]
        #
        # UtilsController.php:#[Route('/utils')]
        # ('/import_csv', name: 'app_import_csv', methods: ['GET'])]
        # ('/do_import_csv', name: 'app_do_import_csv', methods: ['POST'])]
        # ('/backup', name: 'app_backup_index', methods: ['GET'])]
        # ('/do_backup', name: 'app_do_backup_index', methods: ['POST'])]
        # ('/custom_css', name: 'app_custom_css', methods: ['GET'])]
        #
        # ReadingController.php:#[Route('/read')]
        # ('/empty', name: 'app_read_empty', methods: ['GET'])]
        # ('/flashcopied', name: 'app_read_flashcopied', methods: ['GET'])]
        # ('/shortcuts', name: 'app_read_shortcuts', methods: ['GET'])]
        # ('/{TxID}', name: 'app_read', methods: ['GET'])]
        # ('/text/{TxID}', name: 'app_read_text', methods: ['GET'])]
        # ('/sentences/{TxID}', name: 'app_read_sentences', methods: ['GET'])]
        # ('/termform/{LgID}/{text}', name: 'app_term_load', methods: ['GET', 'POST'])]
        # ('/{TxID}/allknown/{nexttextid?}', name: 'app_read_allknown', methods: ['POST'])]
        # ('/{TxID}/goto/{nexttextid}', name: 'app_read_done_goto', methods: ['POST'])]
        # ('/update_status', name: 'app_read_update_status', methods: ['POST'])]

        # Fallthrough: go to lute dev.
        location / {
            proxy_pass http://localhost:9999;
        }
        
        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        #location ~ \.php$ {
        #    root           html;
        #    fastcgi_pass   127.0.0.1:9000;
        #    fastcgi_index  index.php;
        #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        #    include        fastcgi_params;
        #}

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }


    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}


    # HTTPS server
    #
    #server {
    #    listen       443 ssl;
    #    server_name  localhost;

    #    ssl_certificate      cert.pem;
    #    ssl_certificate_key  cert.key;

    #    ssl_session_cache    shared:SSL:1m;
    #    ssl_session_timeout  5m;

    #    ssl_ciphers  HIGH:!aNULL:!MD5;
    #    ssl_prefer_server_ciphers  on;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}
    include servers/*;
}
