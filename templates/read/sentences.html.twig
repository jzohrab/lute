{% for para in paragraphs %}

<p>

  {% for sentence in para %}

  <span class="textsentence" id="sent_{{ sentence.SeID }}">
    {# Note: do not add paragraphs between the renders, it messes up some online chinese dictionary tools. #}
    {% for item in sentence.renderable %}{% include '/read/textitem.html.twig' %}{% endfor %}
  </span>

  {% endfor %}

</p>

{% endfor %}

<script>
  // Defined in lute.js
  parent.start_hover_mode(false);
</script>
