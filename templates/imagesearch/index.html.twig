<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    <title>Images</title>
  </head>

  <script>
    function save_image_locally(src, langid, text) {
      alert(`saving ${src} for ${langid}/${text}`);
    }
  </script>

  <body>
    <p>{{ langid }}, {{ text }}</p>

    {% for image in images %}
    <hr />
    {{ image['src'] }}
    <br />
    <span onclick='save_image_locally("{{ image['src'] }}", {{ langid }}, "{{ text }}")'>
      {{ image['html']|raw }}
    </span>
    {% endfor %}
  </body>

</html>
