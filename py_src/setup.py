"""
Setup so that lutev3 can be "installed" locally for testing.

This shouldn't be needed for production.
"""

from setuptools import setup

setup(
    name="lutev3", version="0.1", packages=["sample"],
)
